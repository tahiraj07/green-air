import { AttributesDirective } from './attributes.directive';

xdescribe('AttributesDirective', () => {
  it('should create an instance', () => {
    // const directive = new AttributesDirective();
    // expect(directive).toBeTruthy();
  });
});
